namespace ThinGin.Core.Common
{
    public static class QuaternionE
    {

        ///// <summary>
        ///// multiplies a given quaternion by a given rotation vector
        ///// </summary>
        ///// <param name="vector"></param>
        ///// <returns></returns>
        //public static Quaternion Multiply(Quaternion quat, Vector3 vector)
        //{
        //    var w = (-quat.X * vector.X) - (quat.Y * vector.Y) - (quat.Z * vector.Z);
        //    var x = (quat.W * vector.X) + (quat.Y * vector.Z) - (quat.Z * vector.Y);
        //    var y = (quat.W * vector.Y) + (quat.Z * vector.X) - (quat.X * vector.Z);
        //    var z = (quat.W * vector.Z) + (quat.X * vector.Y) - (quat.Y * vector.X);

        //    return new Quaternion(x, y, z, w);
        //}
    }
}
